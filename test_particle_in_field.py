import pytestimport qtoml as tomlfrom turbopy import Simulation, PhysicsModule, construct_simulation_from_toml,Gridfrom particle_in_field import EMWave, ChargedParticle, ParticleDiagnosticfrom particle_in_field import FieldPlottingDiagnostic, ForwardEuler    wave_data = {"amplitude" : 1, "omega": 1}owner_data = {"N": 10, "min": 20, "max": 30}@pytest.fixture(name="EM_example")def fixture_example():    Owner = Simulation({"Grid": owner_data})    Owner.read_grid_from_input()    return EMWave(Owner, wave_data)def test_EMWave(EM_example):    assert isinstance(EM_example, EMWave)